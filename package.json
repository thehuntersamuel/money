{
  "name": "pact-finance-morrow",
  "private": true,
  "scripts": {"test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests -p 'test_*.py'"},
  "devDependencies": {"@electric-sql/pglite": "0.5.8"}
}
